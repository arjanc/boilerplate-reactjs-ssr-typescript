import * as React from 'react';

export const Home = () => {
  return (
    <div>
      <h1>HOME</h1>
      <button onClick={() => console.log('Hi there!')}>Press Me!</button>
    </div>
  );
};

export default Home;